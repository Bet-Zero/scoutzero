rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: All operations allowed for emulator testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
