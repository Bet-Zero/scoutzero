# TM_VALIDATOR_TS_NEXT_SCOPE_EXPANSION_AUDIT_E104 — EXECUTION RETURN PACKAGE

## 1. Summary
- Files changed in E104:
  - `return_packages/trade_machine/TM_VALIDATOR_TS_NEXT_SCOPE_EXPANSION_AUDIT_E104_RETURN_PACKAGE.md`
  - `docs/architect/TRADE_MACHINE_MASTER.md`
- E104 re-ran the full two-lane next-scope comparison after E103 and current repo evidence shows the frontier has now pivoted to `surgical-by-default`.
- Batching no longer wins by default after E103. The best next move is the paired `TradeEditor.jsx + TradeTeamCard.jsx` boundary, not `mutationPipeline.js` and not a remaining low-risk batch.
- Strongest Lane A target: the paired orchestration boundary:
  - `src/features/architect/tradeMachine/TradeEditor.jsx`
  - `src/features/architect/tradeMachine/TradeTeamCard.jsx`
- Strongest Lane B target: the mixed shared-display/support trio:
  - `src/features/architect/shared/ValidationWarnings/ValidationWarnings.jsx`
  - `src/features/architect/shared/LeagueView/LeagueView.jsx`
  - `src/features/architect/shared/RosterVisual/RosterVisual.jsx`
- Final recommendation:
  - scope: `TradeEditor + TradeTeamCard paired orchestration boundary`
  - lane: `high-risk surgical`
  - estimated live JS/JSX/TSX business-logic count: `2`
  - likely future execution shape: `one named boundary executed in internal sub-arcs`

## 2. Closed Scope Confirmation
- All prior closed scopes through E103 remain closed/complete under current repo evidence:
  - E39
  - E41
  - E43/E44 `tradeContext`
  - E46 helper foundation
  - E48 `capTotals`
  - E50 `persistenceContracts`
  - E52 season-transition helpers
  - E54 exception-history helpers
  - E56/E57 `playerRulesProfile`
  - E59 contract/season helpers
  - E61/E62 non-trade cap-legality
  - E64 world-aware loader
  - E66/E67 entitlement presentation
  - E69 Trade Machine validation snapshot/accessor
  - E71 Architect contract/cap hook
  - E73 world lifecycle
  - E75 trade-execution helpers
  - E77 helper-trio
  - E78 `useTradeMachine`
  - E80 consent helpers
  - E82 world/data-access helper boundary
  - E84 Team History surface
  - E86 Free Agent Pool surface
  - E88 Cap Sheet display-core
  - E89 Cap Sheet modal pair
  - E91 Free Agency offer-sheet surface
  - E93 Offseason preview surface
  - E95 `seasonManager`
  - E97 Trade Machine validator/result-presentation family
  - E99 Trade Machine preview/export family
  - E101 Trade Team Card leaf family
  - E103 GM world-support family
- Same-path `.js/.jsx` files that already have `.ts/.tsx` authorities remain classified as `TS-backed shim`, not reopened scope. This includes the E91, E93, E95, E97, E99, E101, and E103 same-path holdouts.
- Top-level re-export stubs, barrels, and section shells remain classified as wrapper/shell/compatibility surfaces and stay excluded from the live-business-logic winner count.
- Current frontier bucket rebuild from repo inspection:
  - `high-risk state/orchestration hub`
    - `src/features/architect/tradeMachine/TradeEditor.jsx`
    - `src/features/architect/tradeMachine/TradeTeamCard.jsx`
    - `src/features/architect/utils/mutationPipeline.js`
    - `src/features/architect/GMDashboard/GMDashboard.jsx`
    - `src/features/architect/GMDashboard/components/WorldSelector.jsx`
    - `src/features/architect/GMDashboard/components/SeasonAdvanceModal.jsx`
    - `src/shared/components/EditContractModal.jsx`
  - `low-risk presentational component`
    - `src/features/architect/shared/ValidationWarnings/ValidationWarnings.jsx`
  - `live business logic`
    - `src/features/architect/shared/LeagueView/LeagueView.jsx`
    - `src/features/architect/shared/RosterVisual/RosterVisual.jsx`
    - `src/shared/utils/contracts/contractUtils.js`
    - `src/shared/utils/contracts/seasonNormalizer.js`
  - `TS-backed shim`
    - `src/features/architect/GMDashboard/components/DeleteWorldModal.jsx`
    - `src/features/architect/GMDashboard/components/WorldTimeControls.jsx`
    - `src/features/architect/GMDashboard/components/DraftPositionsInput.jsx`
    - `src/features/architect/GMDashboard/components/OfferSheetList.jsx`
    - `src/features/architect/GMDashboard/sections/FreeAgencySection.jsx`
    - same-path E101 Trade Team Card leaf shims
    - same-path E99 preview/export shims
    - same-path JS helper surfaces with `.ts/.tsx` authorities such as `src/features/architect/utils/contractUtils.js`, `src/features/architect/utils/seasonManager.js`, `src/features/architect/utils/worldManager.js`, `src/features/architect/utils/tradeMachine/engine/tradeValidator.js`, and the closed Trade Machine rules/utilities
  - `barrel/public entrypoint`
    - `src/features/architect/GMDashboard/index.jsx`
    - `src/features/architect/GMDashboard/components/index.js`
    - `src/shared/utils/contracts/index.js`
    - `src/features/architect/utils/architectCore.js`
  - `thin wrapper/deprecated wrapper`
    - `src/features/architect/ValidationWarnings.jsx`
    - `src/features/architect/LeagueView.jsx`
    - `src/features/architect/RosterVisual.jsx`
    - `src/features/architect/GMDashboard/sections/TradeSection.jsx`
    - `src/features/architect/GMDashboard/sections/OffseasonSection.jsx`
  - `debug/support/monitoring`
    - `src/features/architect/utils/tradeMachine/engine/validatorFactory.js`
    - `src/features/architect/utils/tradeMachine/utils/resolveValidationEntitlements.js`
  - `dead/scratch/zero-import residue`
    - `src/features/architect/hooks/useCapSheetState.js`
    - `src/features/architect/utils/cashUtils.js`
    - `src/features/architect/utils/freeAgentLogic.js`
    - `src/features/architect/utils/temp_mutation_code.js`
    - `src/features/architect/utils/tradeMachine/rules/enforcementValidation.js`
    - `src/features/architect/utils/validatePhase21.test.js`
    - `src/features/architect/utils/draftPickUtils.js`
- Zero-import / weak-import residue confirmation before exclusion:
  - `useCapSheetState.js`: still present at 538 lines but no live importers found in `src`; excluded as zero-import residue.
  - `cashUtils.js`: still present at 30 lines with no live importers found in `src`; excluded as zero-import residue.
  - `freeAgentLogic.js`: still present at 84 lines with no live importers found in `src`; excluded as zero-import residue.
  - `temp_mutation_code.js`: still present at 100 lines and reads as scratch logic rather than a production boundary; excluded as dead/scratch residue.
  - `resolveValidationEntitlements.js`: still present at 118 lines but no live importer found in `src`; classified as debug/support residue, not next-scope business logic.
  - `enforcementValidation.js`: still present at 310 lines but no live importer found in `src`; excluded as zero-import legacy residue.
  - `validatorFactory.js`: still present at 121 lines with self-contained registration code but no live importer found in `src`; excluded as support residue.
  - `architectCore.js`: still present at 52 lines as a barrel-only export surface with guardrail-only references, not standalone business logic.
  - `validatePhase21.test.js`: still present at 151 lines as a colocated test file, not a migration candidate.
  - `draftPickUtils.js`: still present at 39 lines and now has test-only usage. Current runtime logic is inline in `validateStepien.ts`, so `draftPickUtils.js` no longer has a live production importer.

## 3. Candidate Next Scopes
- `TradeEditor.jsx`
  - lane: `high-risk surgical`
  - includes:
    - `src/features/architect/tradeMachine/TradeEditor.jsx`
  - excludes:
    - `src/features/architect/tradeMachine/TradeTeamCard.jsx`
    - closed E101 leaf-family shims/authorities
    - `src/features/architect/utils/mutationPipeline.js`
  - estimated live business-logic count: `1`
  - why it is a serious candidate:
    - live `682`-line trade UI hub
    - direct import path is narrow: `TradeSection.jsx -> TradeEditor.jsx`
    - owns preview modal, entitlement editing entrypoints, vacuum-mode cleanup, and trade-apply wiring
  - why it is not the best next move:
    - most hard prop and callback typing still crosses into `TradeTeamCard.jsx`
    - converting it alone would force the highest-friction contracts to remain on the JS side of the boundary

- `TradeTeamCard.jsx`
  - lane: `high-risk surgical`
  - includes:
    - `src/features/architect/tradeMachine/TradeTeamCard.jsx`
  - excludes:
    - `src/features/architect/tradeMachine/TradeEditor.jsx`
    - closed E101 leaf-family shims/authorities
    - `src/features/architect/utils/mutationPipeline.js`
  - estimated live business-logic count: `1`
  - why it is a serious candidate:
    - live `932`-line trade card hub
    - consumes the already-closed E101 leaf family
    - centralizes salary matching, validator snapshot display, entitlement routing, and TPE presentation
  - why it is not the best next move:
    - parent-owned state, validation lifecycle, entitlement editing, preview flow, and apply flow remain in `TradeEditor.jsx`
    - converting it alone would leave the hardest parent-child orchestration boundary split across TS and JSX

- `TradeEditor.jsx + TradeTeamCard.jsx`
  - lane: `high-risk surgical`
  - includes:
    - `src/features/architect/tradeMachine/TradeEditor.jsx`
    - `src/features/architect/tradeMachine/TradeTeamCard.jsx`
  - excludes:
    - `src/features/architect/utils/mutationPipeline.js`
    - `src/features/architect/GMDashboard/GMDashboard.jsx`
    - `src/features/architect/GMDashboard/components/WorldSelector.jsx`
    - `src/features/architect/GMDashboard/components/SeasonAdvanceModal.jsx`
    - closed E101 leaf-family shims/authorities
    - closed E97 validator/result-presentation family
    - closed E99 preview/export family
  - estimated live business-logic count: `2`
  - why it is a serious candidate:
    - the import chain is local and clean: `TradeSection.jsx -> TradeEditor.jsx -> TradeTeamCard.jsx`
    - the nearby Trade Team Card leaves are already TS-backed through E101, so the remaining JS frontier is now concentrated in the two orchestration hubs
    - this boundary stays inside the remaining unmigrated trade UI hubs instead of widening into cross-feature world mutation code
  - why it is the best next move:
    - it is the strongest Lane A comparison target
    - it captures the highest-leverage remaining trade UI boundary without jumping into the much broader mutation engine
    - it beats the remaining Lane B batches because it is now the cleanest concentrated live frontier rather than one more low-risk family

- `mutationPipeline.js`
  - lane: `high-risk surgical`
  - includes:
    - `src/features/architect/utils/mutationPipeline.js`
  - excludes:
    - Trade Editor / Trade Team Card UI hubs
    - dashboard/world hubs
  - estimated live business-logic count: `1`
  - why it is a serious candidate:
    - live `4589`-line engine
    - exports multiple public mutation entrypoints including world mutation apply/compute helpers and event payload construction
    - imported by `useArchitectActions.ts`, `seasonManager.ts`, and many guardrail tests
  - why it is not the best next move:
    - it spans trade, free agency, season advance, offer sheets, persistence, and event payload generation
    - it has the largest blast radius of any remaining single file
    - its likely future execution shape is internal slices, not one-shot conversion

- `GMDashboard.jsx + WorldSelector.jsx + SeasonAdvanceModal.jsx`
  - lane: `high-risk surgical`
  - includes:
    - `src/features/architect/GMDashboard/GMDashboard.jsx`
    - `src/features/architect/GMDashboard/components/WorldSelector.jsx`
    - `src/features/architect/GMDashboard/components/SeasonAdvanceModal.jsx`
  - excludes:
    - closed E103 world-support family
    - `src/features/architect/GMDashboard/sections/OffseasonSection.jsx`
    - `src/features/architect/utils/mutationPipeline.js`
  - estimated live business-logic count: `3`
  - why it is a serious candidate:
    - this is the other major remaining UI hub cluster
    - `WorldSelector.jsx` and `SeasonAdvanceModal.jsx` are both large live JSX components next to `GMDashboard.jsx`
  - why it is not the best next move:
    - the cutoff is less clean
    - `WorldSelector.jsx` depends on the already-closed `DeleteWorldModal.tsx`
    - `SeasonAdvanceModal.jsx` enters through `OffseasonSection.jsx`
    - the grouped boundary sits next to live dashboard shell wiring and world-state hooks rather than an already-clean local pair

- `Shared display/support trio`
  - lane: `batched low-risk`
  - includes:
    - `src/features/architect/shared/ValidationWarnings/ValidationWarnings.jsx`
    - `src/features/architect/shared/LeagueView/LeagueView.jsx`
    - `src/features/architect/shared/RosterVisual/RosterVisual.jsx`
  - excludes:
    - wrapper stubs `src/features/architect/ValidationWarnings.jsx`, `src/features/architect/LeagueView.jsx`, `src/features/architect/RosterVisual.jsx`
  - estimated live business-logic count: `3`
  - why it is a serious candidate:
    - this is the strongest remaining Lane B comparator
    - all three files are still live JS/JSX and have relatively narrow importer footprints
  - why it is not the best next move:
    - `ValidationWarnings.jsx` is a small presentational leaf
    - `LeagueView.jsx` includes live loading and navigation
    - `RosterVisual.jsx` includes roster-building transforms
    - the trio is too behaviorally mixed and scattered to keep batching as the default mode

- `Shared contract pocket`
  - lane: `high-risk surgical`
  - includes:
    - `src/shared/components/EditContractModal.jsx`
    - `src/shared/utils/contracts/contractUtils.js`
    - `src/shared/utils/contracts/seasonNormalizer.js`
  - excludes:
    - architect `src/features/architect/utils/contractUtils.ts` authority and its JS shim
    - dashboard/world hubs
    - Trade Editor / Trade Team Card pair
  - estimated live business-logic count: `3`
  - why it is a serious candidate:
    - the contract utilities are still live JS
    - `EditContractModal.jsx` is shared by Trade Editor, GM Dashboard, and Free Agent Pool
  - why it is not the best next move:
    - it is not a true low-risk batch because `EditContractModal.jsx` is a `1570`-line live modal shared across multiple features
    - this area now reads as a future surgical candidate, not the remaining Lane B winner

- `Wrapper/support cleanup pockets`
  - lane: `batched low-risk`
  - includes:
    - `src/features/architect/ValidationWarnings.jsx`
    - `src/features/architect/LeagueView.jsx`
    - `src/features/architect/RosterVisual.jsx`
    - `src/features/architect/GMDashboard/sections/TradeSection.jsx`
    - `src/features/architect/GMDashboard/sections/OffseasonSection.jsx`
    - `src/features/architect/GMDashboard/index.jsx`
    - `src/features/architect/GMDashboard/components/index.js`
    - `src/shared/utils/contracts/index.js`
  - excludes:
    - underlying live implementation files
  - estimated live business-logic count: `0`
  - why it is a serious candidate:
    - explicit cleanup-only residue still exists
  - why it is not the best next move:
    - these are wrappers, barrels, and shells rather than a real business-logic frontier
    - they are valid non-winners only, not a migration boundary that should beat the remaining surgical work

## 4. Recommended Next Scope
- exact recommended scope name: `TradeEditor + TradeTeamCard paired orchestration boundary`
- exact lane: `high-risk surgical`
- estimated live JS/JSX/TSX business-logic count: `2`
- why it wins:
  - it is the strongest Lane A comparison target from current repo state
  - the import chain is local and explicit: `TradeSection.jsx -> TradeEditor.jsx -> TradeTeamCard.jsx`
  - nearby Trade Team Card leaves are already TS-backed, so the remaining trade UI frontier is concentrated in the two parent hubs
  - it beats `mutationPipeline.js` because the mutation engine is broader, riskier, and more cross-feature
  - it beats the remaining Lane B batches because none of them remain coherent enough to keep batching as the default mode
- recommended execution shape:
  - `one named boundary executed in internal sub-arcs`
  - this should stay a single named migration boundary even if implementation breaks it into internal typing passes
- explicit blocker / widening rule:
  - do not silently widen into:
    - `src/features/architect/utils/mutationPipeline.js`
    - `src/features/architect/GMDashboard/GMDashboard.jsx`
    - `src/features/architect/GMDashboard/components/WorldSelector.jsx`
    - `src/features/architect/GMDashboard/components/SeasonAdvanceModal.jsx`
  - widen only if execution uncovers a concrete typing blocker that cannot be contained with file-local types, and document that blocker explicitly instead of auto-expanding scope

## 5. Live JS/JSX/TSX Business-Logic Inventory For Recommended Scope
- Counted recommended live business logic:
  - `src/features/architect/tradeMachine/TradeEditor.jsx`
    - bucket: `high-risk state/orchestration hub`
    - size: `682` lines
    - live importer evidence:
      - `src/features/architect/GMDashboard/sections/TradeSection.jsx`
    - current role:
      - owns trade preview/open-close state
      - owns entitlement authoring entrypoints
      - wires `useTradeMachine`
      - owns apply/export/reset flow
      - owns the child contract with `TradeTeamCard.jsx`
  - `src/features/architect/tradeMachine/TradeTeamCard.jsx`
    - bucket: `high-risk state/orchestration hub`
    - size: `932` lines
    - live importer evidence:
      - `src/features/architect/tradeMachine/TradeEditor.jsx`
    - current role:
      - consumes the E101 TS-backed leaf family
      - renders per-team trade state
      - owns tab/display state
      - handles salary matching display, validator snapshot reads, entitlement routing, and TPE display logic
- Explicit exclusions from the recommended count:
  - `src/features/architect/utils/mutationPipeline.js`
  - `src/features/architect/GMDashboard/GMDashboard.jsx`
  - `src/features/architect/GMDashboard/components/WorldSelector.jsx`
  - `src/features/architect/GMDashboard/components/SeasonAdvanceModal.jsx`
  - all closed-scope same-path shims from E91, E93, E95, E97, E99, E101, and E103
  - wrapper/barrel/shell surfaces such as `TradeSection.jsx`, `OffseasonSection.jsx`, `GMDashboard/index.jsx`, and `GMDashboard/components/index.js`

## 6. Validation / Inspection Run
- Frontier listing / repo inventory:
  - `rg --files src/features/architect src/shared return_packages/trade_machine docs/architect | rg 'TradeEditor|TradeTeamCard|mutationPipeline|GMDashboard|WorldSelector|SeasonAdvanceModal|ValidationWarnings|LeagueView|RosterVisual|EditContractModal|contractUtils|seasonNormalizer|useCapSheetState|cashUtils|freeAgentLogic|temp_mutation_code|resolveValidationEntitlements|enforcementValidation|validatorFactory|architectCore|validatePhase21|draftPickUtils|TRADE_MACHINE_MASTER|TM_VALIDATOR_TS_GM_WORLD_SUPPORT_FAMILY_E103_RETURN_PACKAGE'`
  - used to rebuild the current E104 frontier after E103
- Importer scans:
  - `rg -n \"TradeEditor|TradeTeamCard|mutationPipeline|GMDashboard|WorldSelector|SeasonAdvanceModal|ValidationWarnings|LeagueView|RosterVisual|EditContractModal|contractUtils|seasonNormalizer|useCapSheetState|cashUtils|freeAgentLogic|temp_mutation_code|resolveValidationEntitlements|enforcementValidation|validatorFactory|architectCore|validatePhase21|draftPickUtils\" src --glob '!**/*.test.*'`
  - targeted follow-up `rg -n` scans for direct import paths, zero-import confirmation, and test-only usage
- Targeted reads:
  - `sed -n` reads were used on:
    - `TradeEditor.jsx`
    - `TradeTeamCard.jsx`
    - `mutationPipeline.js`
    - `GMDashboard.jsx`
    - `WorldSelector.jsx`
    - `SeasonAdvanceModal.jsx`
    - `shared/ValidationWarnings/ValidationWarnings.jsx`
    - `shared/LeagueView/LeagueView.jsx`
    - `shared/RosterVisual/RosterVisual.jsx`
    - `EditContractModal.jsx`
    - `shared/utils/contracts/contractUtils.js`
    - `shared/utils/contracts/seasonNormalizer.js`
    - all required zero-import / weak-import residue files
- Size comparison:
  - `wc -l` was used to compare the main candidate files and residue list
  - key results:
    - `TradeEditor.jsx`: `682`
    - `TradeTeamCard.jsx`: `932`
    - `mutationPipeline.js`: `4589`
    - `GMDashboard.jsx`: `495`
    - `WorldSelector.jsx`: `798`
    - `SeasonAdvanceModal.jsx`: `781`
    - `EditContractModal.jsx`: `1570`
- Same-path shim detection:
  - a repo-wide `.js/.jsx` to `.ts/.tsx` sibling scan was used to confirm which files are already TS-backed shims and must stay excluded from the new live-business-logic count
- Zero-import / weak-import confirmation:
  - direct `rg -n` scans across `src`, `tests`, and `docs` were used to confirm:
    - zero-import residue
    - support-only references
    - `draftPickUtils.js` test-only status
- Required validation commands:
  - `npm run typecheck`
    - Result: PASS
  - `npm run validate:project`
    - Result: PASS
- Broader test suites:
  - no broader tests were needed because the scope decision was resolved by static inspection plus the required repo validations
  - no broader tests were run
- Intentionally skipped commands:
  - `npm run test:diff`
  - `npm run test:architect`
  - `npm run test:ui`
  - `npm run test:node`
  - skipped because this was a doc-only audit pass and the scope decision was resolved by static inspection plus the required `typecheck` and `validate:project` runs
  - `npm run build`
  - skipped because no runtime code changed and the prompt only required the audit docs
  - `npm run lint`
  - skipped because AGENTS.md says to run it only when asked and the repo already carries pre-existing lint noise

## 7. Complexity / Risk Assessment
- Current repo evidence shows the frontier has pivoted to `surgical-by-default`.
- The best next move is the paired `TradeEditor.jsx + TradeTeamCard.jsx` boundary.
- Why the pivot happened:
  - the remaining easy grouped families have already been harvested through E103
  - the strongest remaining Lane B batch is now a mixed trio rather than a clean single family
  - the remaining high-value frontier is concentrated in a few large orchestration hubs
- Why the paired trade boundary beats `mutationPipeline.js`:
  - it is still high-risk, but the blast radius is confined to the remaining trade UI pair
  - `mutationPipeline.js` remains the broader cross-feature engine and should not be the default next move while a cleaner local surgical pair still exists
- Why Lane B loses:
  - the shared-display/support trio is no longer coherent enough to justify batching as the default strategy
  - the contract pocket is dominated by a large shared modal and is not truly low-risk
- Future execution guidance:
  - stay inside one named boundary executed in internal sub-arcs
  - do not reopen batching as the default strategy from this point unless a future audit finds a new genuinely coherent low-risk family
  - do not silently widen into `mutationPipeline.js` or the dashboard/world hubs

## 8. Master Doc Update
- Add `### Validator TS Next-Scope Expansion Audit E104 (2026-03-15)` to `docs/architect/TRADE_MACHINE_MASTER.md` immediately after the E103 entry.
- Record that all prior closed scopes through E103 remain closed/complete.
- Record that the strongest Lane A comparison target is `TradeEditor.jsx + TradeTeamCard.jsx`.
- Record that the strongest Lane B comparison target is the shared-display/support trio `ValidationWarnings/LeagueView/RosterVisual`.
- Record that the final recommended next scope is `TradeEditor + TradeTeamCard paired orchestration boundary`.
- Record that the chosen lane is `high-risk surgical`.
- Record that the estimated live business-logic count is `2`.
- Record that the likely future execution shape is `one named boundary executed in internal sub-arcs`.
- Record plainly that the repo has now pivoted to `surgical-by-default`.
- Record validation:
  - `npm run typecheck: PASS`
  - `npm run validate:project: PASS`
- Point the master doc entry to:
  - `return_packages/trade_machine/TM_VALIDATOR_TS_NEXT_SCOPE_EXPANSION_AUDIT_E104_RETURN_PACKAGE.md`
