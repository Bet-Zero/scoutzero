# TM_VALIDATOR_TS_TRADE_TEAM_CARD_LEAF_FAMILY_E101 — EXECUTION RETURN PACKAGE

## 1. Summary
- Migrated the counted 7-file Trade Team Card leaf family to authoritative `.tsx` implementations:
  - `src/features/architect/tradeMachine/CapImpactTiles.tsx`
  - `src/features/architect/tradeMachine/SelectTeamCard.tsx`
  - `src/features/architect/tradeMachine/OutgoingPlayersList.tsx`
  - `src/features/architect/tradeMachine/TradePlayerRow.tsx`
  - `src/features/architect/tradeMachine/EntitlementPicksList.tsx`
  - `src/features/architect/tradeMachine/EntitlementPickRow.tsx`
  - `src/features/architect/tradeMachine/TradeExceptionManager.tsx`
- Behavior was preserved inside the E101 boundary:
  - exact export shapes stayed intact
  - same-path `.jsx` files were reduced to shim-only compatibility surfaces
  - list/row boundaries, menu wording/order, callback names, callback argument order, sign-and-trade gating, contract-edit behavior, entitlement routing, undo/remove behavior, and vacuum-session actions remained unchanged
- No in-scope runtime logic had to remain in JS/JSX. The only directly related JS/JSX left in the family are same-path `.jsx` shims kept for compatibility.
- `TradeTeamCard.jsx`, `TradeEditor.jsx`, `mutationPipeline.js`, the closed E97 family, the closed E99 family, and dashboard hubs were not touched.

## 2. Files Changed
- `src/features/architect/tradeMachine/CapImpactTiles.tsx`
  - Added the authoritative TS-backed cap-impact tile implementation.
  - Safe because it preserves the existing render tree, labels, tile order, fallback math, validator snapshot behavior, and validating/cap-holds messaging.
- `src/features/architect/tradeMachine/SelectTeamCard.tsx`
  - Added the authoritative TS-backed team-selection leaf.
  - Safe because it keeps the same named-only export surface, the same option source, and the same `onSelectTeam` / `onRemove` behavior.
- `src/features/architect/tradeMachine/TradeExceptionManager.tsx`
  - Added the authoritative TS-backed TPE display leaf.
  - Safe because it preserves active/expired grouping, wording, formatting, filtering, and default export behavior.
- `src/features/architect/tradeMachine/OutgoingPlayersList.tsx`
  - Added the authoritative TS-backed outgoing-player list.
  - Safe because it preserves incoming-first plus salary sorting, menu coordination, callback propagation, and named-only export shape.
- `src/features/architect/tradeMachine/TradePlayerRow.tsx`
  - Added the authoritative TS-backed player row.
  - Safe because it preserves exact menu wording/order, callback contracts, S&T gating, undo behavior, contract-edit behavior, compact/standard row layouts, and profile navigation wiring.
- `src/features/architect/tradeMachine/EntitlementPicksList.tsx`
  - Added the authoritative TS-backed entitlement list.
  - Safe because it preserves pooled filtering, identity-key dedupe, year grouping, sort order, create-button behavior, callback propagation, and default-plus-named export shape.
- `src/features/architect/tradeMachine/EntitlementPickRow.tsx`
  - Added the authoritative TS-backed entitlement row.
  - Safe because it preserves exact menu wording/order, routing behavior, edit/view/revert/delete callbacks, vacuum-session actions, and pick-row projection display.
- `src/features/architect/tradeMachine/CapImpactTiles.jsx`
  - Replaced the old JSX implementation with a shim-only default re-export to `CapImpactTiles.tsx`.
  - Safe because exact-path compatibility remains intact and the file no longer carries live business logic.
- `src/features/architect/tradeMachine/SelectTeamCard.jsx`
  - Replaced the old JSX implementation with a shim-only named re-export to `SelectTeamCard.tsx`.
  - Safe because the named-only module surface remains unchanged.
- `src/features/architect/tradeMachine/TradeExceptionManager.jsx`
  - Replaced the old JSX implementation with a shim-only default re-export to `TradeExceptionManager.tsx`.
  - Safe because explicit `.jsx` import compatibility remains intact.
- `src/features/architect/tradeMachine/OutgoingPlayersList.jsx`
  - Replaced the old JSX implementation with a shim-only named re-export to `OutgoingPlayersList.tsx`.
  - Safe because the named-only module surface remains unchanged.
- `src/features/architect/tradeMachine/TradePlayerRow.jsx`
  - Replaced the old JSX implementation with a shim-only default re-export to `TradePlayerRow.tsx`.
  - Safe because exact-path compatibility remains intact while runtime authority moved to TS.
- `src/features/architect/tradeMachine/EntitlementPicksList.jsx`
  - Replaced the old JSX implementation with a shim-only default-plus-named re-export to `EntitlementPicksList.tsx`.
  - Safe because both export channels remain available exactly as before.
- `src/features/architect/tradeMachine/EntitlementPickRow.jsx`
  - Replaced the old JSX implementation with a shim-only default re-export to `EntitlementPickRow.tsx`.
  - Safe because exact-path compatibility remains intact while runtime authority moved to TS.
- `src/tests/architect/tradeTeamCardLeafFamily.compatibility.guardrail.test.tsx`
  - Added focused E101 compatibility coverage for shim purity and exact export-surface preservation.
  - Safe because it proves the compatibility contract without changing runtime behavior.
- `src/tests/architect/tradeTeamCardLeafFamily.display.e101.test.tsx`
  - Added focused display coverage for `CapImpactTiles`, `SelectTeamCard`, and `TradeExceptionManager`.
  - Safe because it verifies leaf display semantics only.
- `src/tests/architect/tradeTeamCardLeafFamily.rowContracts.e101.test.tsx`
  - Added focused row/list coverage for exact menu wording/order, callback argument order, and list composition behavior.
  - Safe because it proves existing UI contracts without altering implementation.
- `src/tests/architect/phase73_tile_reactivity_and_totals_drift_guardrails.test.js`
  - Retargeted the source scan to `CapImpactTiles.tsx` and added a shim-only assertion for `CapImpactTiles.jsx`.
  - Safe because it keeps the existing guardrail intent aligned with the new authority/shim split.
- `src/tests/architect/noVacuumWording.test.ts`
  - Retargeted the user-visible-copy scan to `EntitlementPicksList.tsx` and `EntitlementPickRow.tsx`.
  - Safe because it preserves the copy guardrail while following the new authoritative files.
- `src/tests/architect/tradePlayerRow.signAndTradeInjector.test.tsx`
  - Added `cleanup()` isolation between tests.
  - Safe because it fixed stale-DOM leakage in the existing focused row test without changing the component contract.
- `docs/architect/TRADE_MACHINE_MASTER.md`
  - Added the indexed E101 entry with scope, outcome, validation results, and post-E101 status.
  - Safe because it is documentation only.

## 3. Types Introduced or Hardened
- `CapImpactTilesProps`, `TradeAssetLike`, `TeamLike`, `SnapshotLike`
  - File-local permissive types for cap tile inputs and validator snapshot access.
  - Apply only inside `src/features/architect/tradeMachine/CapImpactTiles.tsx`.
- `SelectTeamCardProps`
  - File-local permissive props for select/remove callbacks.
  - Applies only inside `src/features/architect/tradeMachine/SelectTeamCard.tsx`.
- `TradeExceptionManagerProps`, `TradeExceptionLike`
  - File-local permissive TPE display types.
  - Apply only inside `src/features/architect/tradeMachine/TradeExceptionManager.tsx`.
- `OutgoingPlayersListProps`, `PlayerLike`, `TeamLike`, `TeamOptionLike`
  - File-local permissive list/row handoff types for outgoing-player composition.
  - Apply only inside `src/features/architect/tradeMachine/OutgoingPlayersList.tsx`.
- `TradePlayerRowProps`, `PlayerLike`, `TeamOptionLike`
  - File-local permissive row/menu contract types.
  - Apply only inside `src/features/architect/tradeMachine/TradePlayerRow.tsx`.
- `EntitlementPicksListProps`, `EntitlementLike`, `OutgoingEntitlementLike`, `TeamOptionLike`
  - File-local permissive entitlement-list composition types.
  - Apply only inside `src/features/architect/tradeMachine/EntitlementPicksList.tsx`.
- `EntitlementPickRowProps`, `EntitlementLike`, `TeamOptionLike`, `PickRowLike`
  - File-local permissive entitlement-row menu/routing types.
  - Apply only inside `src/features/architect/tradeMachine/EntitlementPickRow.tsx`.
- No shared/public type barrel was introduced.
  - This was intentional to keep E101 inside the leaf-family boundary and preserve current input looseness.

## 4. Migration Work Completed
- Display leaves
  - Moved `CapImpactTiles`, `SelectTeamCard`, and `TradeExceptionManager` into `.tsx` authorities with unchanged render structures and unchanged export contracts.
  - Authoritative behavior stayed the same for labels, ordering, formatting, fallback handling, and visible status messaging.
  - No contract correction was required.
- Player list/row chain
  - Moved `OutgoingPlayersList` and `TradePlayerRow` into `.tsx` authorities without changing their list/row split.
  - Preserved incoming-first sorting, salary ordering, menu coordination, exact menu wording/order, callback propagation, callback argument order, sign-and-trade behavior, contract editing, undo behavior, and profile navigation.
  - No callback normalization or argument reordering was introduced.
- Entitlement list/row chain
  - Moved `EntitlementPicksList` and `EntitlementPickRow` into `.tsx` authorities without changing their list/row split.
  - Preserved pooled filtering, identity-key dedupe, year grouping, round/kind sorting, menu coordination, routing/select behavior, edit/view/revert/delete behavior, vacuum-session behavior, and projection display semantics.
  - No routing/editing contract correction was required.
- Compatibility and guardrails
  - Converted every same-path `.jsx` file in the family into a shim-only compatibility surface that preserves the exact export shape of its authority file.
  - Added E101 compatibility, display, and row-contract proof files, retargeted two existing guardrails, and fixed a stale-DOM issue in the existing S&T injector test.
  - `TradeTeamCard.jsx` did not need any compile or test fix, so the scope stayed inside the seven-file family.

## 5. JS/JSX Holdouts
- `src/features/architect/tradeMachine/CapImpactTiles.jsx`
  - Remains JSX only as a shim-only compatibility surface by E101 rule.
- `src/features/architect/tradeMachine/SelectTeamCard.jsx`
  - Remains JSX only as a shim-only compatibility surface by E101 rule.
- `src/features/architect/tradeMachine/OutgoingPlayersList.jsx`
  - Remains JSX only as a shim-only compatibility surface by E101 rule.
- `src/features/architect/tradeMachine/TradePlayerRow.jsx`
  - Remains JSX only as a shim-only compatibility surface by E101 rule.
- `src/features/architect/tradeMachine/EntitlementPicksList.jsx`
  - Remains JSX only as a shim-only compatibility surface by E101 rule.
- `src/features/architect/tradeMachine/EntitlementPickRow.jsx`
  - Remains JSX only as a shim-only compatibility surface by E101 rule.
- `src/features/architect/tradeMachine/TradeExceptionManager.jsx`
  - Remains JSX only as a shim-only compatibility surface by E101 rule.
- `src/features/architect/tradeMachine/TradeTeamCard.jsx`
  - Remains live JSX because E101 explicitly excluded the team-card orchestration hub; no blocker forced expansion into it.
- `src/features/architect/tradeMachine/TradeEditor.jsx`
  - Remains live JSX because E101 explicitly excluded the editor/orchestration hub; no blocker forced expansion into it.

## 6. Regression Coverage Run
- `npm run typecheck`
  - Proved the seven new `.tsx` authorities, the kept `.jsx` shims, and the new/retargeted tests all compile cleanly.
  - Result: PASS.
- `npm run test:architect -- --reporter=dot src/tests/architect/tradeTeamCardLeafFamily.compatibility.guardrail.test.tsx src/tests/architect/tradeTeamCardLeafFamily.display.e101.test.tsx src/tests/architect/tradeTeamCardLeafFamily.rowContracts.e101.test.tsx src/tests/architect/phase73_tile_reactivity_and_totals_drift_guardrails.test.js src/tests/architect/noVacuumWording.test.ts src/tests/architect/tradePlayerRow.signAndTradeInjector.test.tsx src/tests/architect/tradePlayerRow.yearsRemainingDisplay.test.tsx src/tests/architect/entitlementPickRowDisplay.test.jsx src/tests/architect/entitlementPickRow.vacuumBadges.test.jsx`
  - Intended to run the narrow architect proof set, but the repo script still seeded the full architect roots.
  - Result: FAIL outside E101 because `src/tests/architect/phase43_apron_drift_prevention_guardrails.test.js` flagged out-of-scope `src/features/architect/tradeMachine/FaExceptionTracker.tsx`.
- `npm run test:node -- --reporter=dot src/tests/architect/phase73_tile_reactivity_and_totals_drift_guardrails.test.js src/tests/architect/noVacuumWording.test.ts`
  - Proved the retargeted node/source-scan guardrails pass cleanly.
  - Result: PASS.
- `npm run test:ui -- --reporter=dot src/tests/architect/tradeTeamCardLeafFamily.compatibility.guardrail.test.tsx src/tests/architect/tradeTeamCardLeafFamily.display.e101.test.tsx src/tests/architect/tradeTeamCardLeafFamily.rowContracts.e101.test.tsx`
  - Proved the new E101 shim/export-surface checks, display checks, list/row composition checks, exact menu wording/order checks, and exact callback argument-order checks.
  - Result: PASS.
- `npm run test:ui -- --reporter=dot src/tests/architect/tradePlayerRow.signAndTradeInjector.test.tsx src/tests/architect/tradePlayerRow.yearsRemainingDisplay.test.tsx src/tests/architect/entitlementPickRowDisplay.test.jsx src/tests/architect/entitlementPickRow.vacuumBadges.test.jsx`
  - Proved the existing focused row tests still pass after E101.
  - Result: PASS.
- `npm run build`
  - Proved the app still builds with the new authorities/shims in place.
  - Result: PASS with pre-existing warnings about stale Browserslist data, `fs` browser externalization from `tradeDebug.js`, mixed static/dynamic imports, and large chunks outside E101.
- `npm run validate:project`
  - Proved the repo structure remains schema-valid.
  - Result: PASS.
- Intentionally skipped:
  - `npm run test:full`
    - Skipped because the prompt did not contain `RUN FULL SUITE`, and AGENTS.md blocks it without that exact phrase.
  - `npm run lint`
    - Skipped because AGENTS.md says to run it only when asked, and the repo already has many pre-existing lint errors.

## 7. Post-E101 Status
- The counted Trade Team Card leaf family is effectively complete as a TS-backed family.
- The grouped batch succeeded cleanly inside the seven-file boundary.
- No mandatory follow-up remains inside the leaf family itself.
- The broader leaf-family boundary is now effectively complete:
  - all seven counted leaf files are TS-backed authorities
  - the only remaining JS/JSX inside that family is shim-only compatibility residue
  - nearby remaining live JS/JSX work lives in excluded orchestration hubs `TradeTeamCard.jsx` and `TradeEditor.jsx`, not inside the counted leaf family
- E101 did not reopen E39, E41, E43/E44, E46, E48, E50, E52, E54, E56/E57, E59, E61/E62, E64, E66/E67, E69, E71, E73, E75, E77, E78, E80, E82, E84, E86, E88, E89, E91, E93, E95, E97, or E99.

## 8. Master Doc Update
- Added `### Validator TS Trade Team Card Leaf Family E101 (2026-03-14)` to `docs/architect/TRADE_MACHINE_MASTER.md` immediately after the E100 entry.
- Recorded that the counted 7-file Trade Team Card leaf family is now TS-backed through `.tsx` authorities, with same-path `.jsx` files retained as shim-only compatibility surfaces.
- Recorded that behavior stayed unchanged across export shape, display semantics, list/row composition, menu wording/order, callback contracts, sign-and-trade behavior, contract editing, entitlement routing, and vacuum-session behavior.
- Recorded the actual validation history, including:
  - passing targeted `test:node`
  - passing targeted `test:ui` for the new E101 tests
  - passing targeted `test:ui` for the existing focused row tests
  - the attempted `test:architect` overrun into a single out-of-scope `FaExceptionTracker.tsx` guardrail failure
  - passing `typecheck`, `build`, and `validate:project`
- Recorded that the grouped batch completed cleanly and that the broader leaf-family boundary is now effectively complete, while `TradeTeamCard.jsx` and `TradeEditor.jsx` remain excluded orchestration hubs.
