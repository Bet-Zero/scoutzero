// Phase 58 compatibility shim. Authoritative implementation lives in ./assertions.ts.
export {
  assertPostTradeSnapshot,
  assertValidatedTradeContext,
  assertTradeComputeInputs,
} from './assertions.ts';
