/**
 * Temporary stub for validateSignAndTrade to fix import issues
 * Original file has syntax errors that need to be resolved
 */

export function validateSignAndTrade(team, tradeCtx = {}) {
  return {
    passed: true,
    violations: [],
    message: 'Sign-and-trade validation temporarily disabled due to syntax errors',
    details: {
      hasSignAndTrade: false,
      hardCapped: false,
    }
  };
}