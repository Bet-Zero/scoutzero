# Schema Changelog

- v1: Initial canonical schemas established for `players_v2` and `architect`.
  - Reconciled contract fields with scraper normalization outputs.
  - Added code-first Zod schemas, JSON Schema generation, and docs.
