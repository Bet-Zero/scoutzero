# Tiermaker/Tieramid Execution Return Package — 2026-02-05 (Execution 1)

## What Changed

- Fixed Tieramid team roster matching to use `teamId`/`code` and added automatic list loading via route `tierListId`.
- Added Tieramid pool rehydration after player data loads with guardrails to avoid overwriting user state.
- Added Tieramid remove-to-pool controls and enforced capacity normalization to prevent hidden players.
- Updated Tiermaker remove behavior to return players to Pool and fixed minor UI class/contrast issues.
- Defined and documented cross-mode compatibility rules and closed related gaps.

## Gap Closure Table

| Gap                                   | Status | Notes                                                                                              |
| ------------------------------------- | ------ | -------------------------------------------------------------------------------------------------- |
| 1) Tieramid Add Team mismatch         | CLOSED | Uses `selectedTeam.teamId` / `selectedTeam.code` and matches on `bio.display.teamId` or team code. |
| 2) Tieramid Pool never rehydrates     | CLOSED | Guarded hydration after player data load when board is empty.                                      |
| 3) Tieramid missing “Remove to Pool”  | CLOSED | Added per-tile remove-to-pool control.                                                             |
| 4) Tiermaker Remove deletes player    | CLOSED | Remove now returns player to Pool with de-dupe.                                                    |
| 5) Tieramid overflow hidden           | CLOSED | Capacity normalization on load; overflow moved to Pool with toast.                                 |
| 6) Cross-mode listId loading          | CLOSED | Route `tierListId` passed to Tieramid and auto-loads list.                                         |
| 7) CreateTierListModal input contrast | CLOSED | Input text/placeholder updated for readability.                                                    |
| 8) Invalid Tailwind class pb-12x      | CLOSED | Replaced with `pb-12` in both boards.                                                              |

## Validation Evidence

- Lint: `npm run lint`
  - Result: **FAILED** (pre-existing repo errors; includes extensive unrelated ESLint violations).
- Tests: `npm run test -- --run`
  - Result: **FAILED**
  - Example failures: `src/tests/trade/TradeValidationGating.guardrail.test.jsx`, `src/tests/trade/staleValidationFix.test.js`.
- Manual UI validation: **NOT PERFORMED** (requires interactive browser session).

## Firestore Notes

- Firestore writes not performed in this session.

## FILES TOUCHED

- src/features/tierMaker/TierMakerBoard.jsx
- src/features/tierMaker/TieramidBoard.jsx
- src/features/tierMaker/CreateTierListModal.jsx
- src/pages/TierMakerView.jsx
- docs/features/tiermaker_tieramid_MASTER.md
- return_packages/tiermaker/2026-02-05_execution-1.md
