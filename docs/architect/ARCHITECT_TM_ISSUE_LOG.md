| Issue ID | Related Step | Severity | Description                                                                                                                    | Status |
| -------- | ------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------ | ------ |
| TM-1A    | TM-1A        | HIGH     | UI preview legality does not represent all apply-time validation gates, so a trade can appear valid but fail during execution. RESOLVED: getFullLegalityPreview now covers CBA snapshot + validatePostStateCapLegality (pure gate, no Firestore). Only 3 Firestore-dependent gates remain apply-only by architectural necessity (validateMutationLeagueInvariants, validateMutationEntitlementInvariants, validateTradeApplyExclusivity). UI disclosure updated to reflect new coverage. | RESOLVED |
| TM-1B    | TM-1B        | MEDIUM   | Roster validation was split across 4 independent implementations with separately defined constants (14/15/3). RESOLVED: ROSTER_LIMITS + checkRosterCounts canonical in validateRoster.ts. Pre-trade path (computeRosterValidation in tradeValidator.ts) now delegates rule-check to checkRosterCounts. Post-state path (postStateCapValidator.ts) imports ROSTER_LIMITS and adds previously missing minimum-roster check (PSV_ROSTER_002). rosterValidation.ts and enforcement.ts local constants replaced with ROSTER_LIMITS aliases. Changing any roster limit is now a single-file edit. | RESOLVED |
| TM-1C    | TM-1C        | MEDIUM   | Hard cap and apron enforcement is distributed across multiple modules without a clear single source of truth. RESOLVED (followup complete): Orphaned validateHardCap.ts deleted. All three behavioral conflicts resolved. (1) basicRules.ts prior-year TPE existence guard removed — validateTradeExceptions.ts is sole canonical authority (usage-only per CBA: holding a prior-year TPE without using it does not block a trade). (2) basicRules.ts multi-player outgoing broad block removed — validateAggregation.ts is sole canonical authority (aggregate-up only; equal-value multi-player trades are CBA-legal). (3) postStateCapValidator.ts resolveHardCapCeiling() now delegates ceiling-value resolution to the exported hardCapStatus.ts:resolveHardCapCeiling(); detection logic (reads post-state afterTeamsByCode / afterTotalsByTeam shapes) retained locally. basicRules.ts now enforces only the cash restriction (true SSOT). Hard cap / apron rule family no longer contains active conflicting live validators. | RESOLVED |
| TM-1D    | TM-1D        | MEDIUM   | An alternate trade execution path may bypass mutation pipeline validation and post-state legality checks. RESOLVED: removed dormant `tradeManager.executeTrade()` and its `architectCore.ts` re-export. Authoritative trade execution is now limited to the mutation pipeline flow: `applyWorldMutation({ mutationType: 'executeTrade', ... })` for world persistence and `computeWorldMutation({ mutationType: 'executeTrade', ... })` for base-state local apply. | RESOLVED |
| TM-3-1   | TM-3A, TM-3B, TM-3E | HIGH | The apply-time legality chain is fragmented across snapshot construction, validateTrade reuse, post-state validation, and world invariant checks with no single orchestration surface. No function exists that a developer can point to as "final trade execution authority." The boundary between UI handoff and persistence is implicit, discoverable only by tracing the full chain manually. This fragmentation makes future execution changes error-prone and difficult to verify for completeness. | PARTIALLY_RESOLVED — TM-3A complete: `validateTradeExecutionAuthority()` in `tradeContext/tradeExecutionAuthority.ts` provides the single named authority surface. Remaining: TM-3B (centralize non-trade paths), TM-3E (document boundary). |
| TM-3-2   | TM-3C        | MEDIUM   | Validation layer ownership is undefined. Rules are distributed across trade-level (validateTrade), post-state (validatePostStateCapLegality), and world-state (validateMutationLeagueInvariants, validateMutationEntitlementInvariants, validateTradeApplyExclusivity) layers without documented ownership boundaries. There is no clear answer to "where does a new rule belong?" — creating duplication risk and increasing the likelihood of rule placement in the wrong layer over time. | OPEN |
| TM-3-3   | TM-3D        | MEDIUM   | Preview flow (getFullLegalityPreview) is structurally misaligned with the execution authority model. Preview simulates apply-path logic but does not mirror the authoritative chain's structure or entry points. The conceptual model used during preview diverges from the model used during apply, making it harder to reason about what preview actually covers and harder to maintain alignment as execution logic evolves. | OPEN |
