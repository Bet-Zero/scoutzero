# Architect Contract Flow Trust Review

Code-trace review originally created on 2026-03-22, updated the same day after E3 execution fixed the trade/sign-and-trade persisted-player-truth blocker at the authoritative mutation layer, updated again on 2026-03-23 after E4 brought offer-sheet finalization onto the same canonical final-snapshot persistence model, updated later on 2026-03-23 after E5 fixed authoritative `storeOfferSheet` ownership and source-player resolution, and updated again on 2026-03-23 after E6 aligned SAT modal preflight with authoritative receiving-team post-trade legality.

## Executive verdict

`NOT TRUSTWORTHY YET`

The authoritative mutation pipeline is strong on sequencing and atomic batch persistence. E3 closed the trade/sign-and-trade persisted-player-truth blocker, E4 did the same for offer-sheet finalization, and E5 closed the remaining offer-sheet creation truth gap: `storeOfferSheet` resolves ownership from authoritative world snapshot membership, loads canonical player truth from the resolved home-team path, and fails closed when ownership or source truth is ambiguous. E6 now closes the SAT modal/preflight drift: the modal uses a shared authoritative SAT preflight backed by the receiving team’s real post-trade context, fails closed when that preflight is unavailable or incomplete, and still defers final authority to apply-time mutation validation. The flow is still not trustworthy end-to-end because the offer-sheet modal still does not use authoritative preflight and the 48-hour match window remains warning-only.

## Files reviewed

- `src/shared/components/EditContractModal.tsx`
- `src/features/architect/GMDashboard/GMDashboard.tsx`
- `src/features/architect/GMDashboard/hooks/useArchitectActions.ts`
- `src/features/architect/GMDashboard/hooks/useArchitectModals.ts`
- `src/features/architect/GMDashboard/sections/FreeAgencySection.tsx`
- `src/features/architect/GMDashboard/components/OfferSheetList.tsx`
- `src/features/architect/freeAgency/FreeAgentPool/FreeAgentPool.tsx`
- `src/features/architect/hooks/useCapValidation.ts`
- `src/features/architect/tradeMachine/TradeEditor.tsx`
- `src/features/architect/utils/mutationPipeline.ts`
- `src/features/architect/utils/capLegalityValidation.ts`
- `src/features/architect/utils/capLegality/postStateCapValidator.ts`
- `src/features/architect/utils/tradeContext/tradeContext.ts`
- `src/features/architect/utils/tradeMachine/engine/tradeValidator.ts`
- `src/features/architect/utils/tradeMachine/rules/validateSignAndTrade.ts`
- `src/features/architect/utils/tradeMachine/signAndTrade/signAndTradeEligibility.ts`
- `src/features/architect/utils/contractNormalization.ts`
- `src/features/architect/utils/persistenceContracts/contracts.ts`
- `src/features/architect/utils/teamLoader.ts`
- `src/features/architect/utils/worldTeamData.ts`

Primary evidence came from live `.ts/.tsx` code paths. Older docs/tests were not used as primary proof for any conclusion below.

## Offer Sheet lifecycle map

| Stage | Entry file/function | Validation file/function | Compute file/function | Persistence file/function | SSOT status | Resulting state change | Failure-path safety | Stage assessment | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1. UI initiation | `EditContractModal.handleConfirm`; `GMDashboard` modal wiring; `FreeAgentPool` modal wiring | `useCapValidation` on generic `signNew` payload only | None | None | `UI-only` | Local `isOfferSheet` toggle only. No world mutation yet. | Safe from partial persistence because nothing is written. Risk is preflight drift: UI legality is computed before offer-sheet flags/status are added. | `Confirmed risk` | `src/shared/components/EditContractModal.tsx:648-671,1111-1124,1519-1535`; `src/features/architect/GMDashboard/GMDashboard.tsx:188-196,540-556`; `src/features/architect/freeAgency/FreeAgentPool/FreeAgentPool.tsx:231-250` |
| 2. Store + mirror | `EditContractModal.handleConfirm` -> `useArchitectActions.handleStoreOfferSheet` | UI: `useCapValidation`; authoritative: `validateMutation('storeOfferSheet')` -> `validateSigning` | `computeStoreOfferSheetResult` | `applyWorldMutation` -> `persistWorldMutation` | `persisted world truth` | Writes outgoing `offerSheets[]` on offering team, mirrors to home-team `incomingOfferSheets[]`, preserves deterministic `dedupKey`, does not touch roster/totals by design. | Atomic batch means no partial mirror writes if commit fails. E5 moved source-of-truth resolution ahead of validation/storage: ownership now resolves from authoritative world snapshot membership with strict precedence `roster` then `players[]`, disagreement fails closed, offering-team-path player reads are not used, and canonical player truth comes only from the resolved home-team snapshot plus home-team override lineage. | `No issue confirmed` | `src/features/architect/GMDashboard/hooks/useArchitectActions.ts:2057-2135`; `src/features/architect/utils/mutationPipeline.ts`; `tests/architect/mutationPipeline.tradePersistenceTruth.test.ts`; `tests/architect/offerSheetPersistence.test.js` |
| 3. Match | `OfferSheetList` Match button -> `useArchitectActions.handleMatchOfferSheet` | `validateOfferSheetResolution(action='match')` | `computeMatchOfferSheetResult` | `applyWorldMutation` -> `persistWorldMutation` | `persisted world truth` | Updates status to `MATCHED` on both offering and home-team mirrored rows. | Atomic dual-team write is safe from partial commit. If later finalization fails, the saved `MATCHED` status is still truthful intermediate state. The main rule gap is that an expired 48-hour match window only emits a warning, so a late match can still persist. | `Confirmed risk` | `src/features/architect/GMDashboard/components/OfferSheetList.tsx:85-126`; `src/features/architect/GMDashboard/hooks/useArchitectActions.ts:2137-2174`; `src/features/architect/utils/mutationPipeline.ts:3957-3974,4508-4586`; `src/features/architect/utils/capLegalityValidation.ts:3958-3980` |
| 4. Decline | `OfferSheetList` Decline button -> `useArchitectActions.handleDeclineOfferSheet` | `validateOfferSheetResolution(action='decline')` | `computeDeclineOfferSheetResult` | `applyWorldMutation` -> `persistWorldMutation` | `persisted world truth` | Updates status to `DECLINED` on both offering and home-team mirrored rows. | Atomic dual-team write is safe from partial commit. If later finalization fails, saved `DECLINED` status remains truthful intermediate state. I did not confirm a separate decline-stage correctness bug beyond the later finalization issues. | `No issue confirmed` | `src/features/architect/GMDashboard/components/OfferSheetList.tsx:85-112`; `src/features/architect/GMDashboard/hooks/useArchitectActions.ts:2176-2213`; `src/features/architect/utils/mutationPipeline.ts:3976-3993,4591-4664`; `src/features/architect/utils/capLegalityValidation.ts:3958-3988` |
| 5. Finalize matched | `OfferSheetList` Finalize Match -> `useArchitectActions.handleFinalizeOfferSheet` -> `finalizeMatchedOfferSheet` | `validateOfferSheetResolution(action='finalize')` | `computeFinalizeMatchedOfferSheetResult` | `applyWorldMutation` -> `persistWorldMutation` | `persisted world truth` | Removes the sheet from both sides, replaces the home-team player with the normalized matched contract in the final home-team snapshot, removes the matching cap hold, and emits a canonical same-team player override upsert. | Commit remains atomic, and E4 changed the compute path to fail closed if the player cannot be resolved on the home-team snapshot or the final snapshot cannot supply a canonical persisted player object. Because persistence is now derived from the final home-team snapshot, downstream `getPlayer` reads stay aligned with saved team truth. | `No issue confirmed` | `src/features/architect/GMDashboard/components/OfferSheetList.tsx:114-126`; `src/features/architect/GMDashboard/hooks/useArchitectActions.ts:2215-2295`; `src/features/architect/utils/mutationPipeline.ts:2384-2488,5210-5363,4653-4868`; `tests/architect/mutationPipeline.tradePersistenceTruth.test.ts`; `tests/architect/offerSheetPersistence.test.js` |
| 6. Finalize declined | `OfferSheetList` Finalize Signing -> `useArchitectActions.handleFinalizeOfferSheet` -> `finalizeDeclinedOfferSheet` | `validateOfferSheetResolution(action='finalize')` | `computeFinalizeDeclinedOfferSheetResult` | `applyWorldMutation` -> `persistWorldMutation` | `persisted world truth` | Removes both mirrored sheets, resolves the canonical home-team player snapshot, writes the normalized declined-offer contract onto the offering team snapshot, removes the player and stale cap holds from the home team, and persists canonical destination truth plus explicit source-team cleanup. | Commit remains atomic, and E4 changed the compute path to fail closed if the source player cannot be resolved on the home-team snapshot or the final destination snapshot cannot supply a canonical persisted player object. The persisted destination override now comes from the final offering-team snapshot rather than offer-sheet fragments, and the superseded old-team override path is deleted in the same batch. | `No issue confirmed` | `src/features/architect/GMDashboard/components/OfferSheetList.tsx:128-140`; `src/features/architect/GMDashboard/hooks/useArchitectActions.ts:2215-2295`; `src/features/architect/utils/mutationPipeline.ts:2071-2186,2384-2488,5365-5510,4653-4868`; `tests/architect/mutationPipeline.tradePersistenceTruth.test.ts`; `tests/architect/offerSheetPersistence.test.js` |
| 7. Idempotency + world persistence envelope | `useArchitectActions.runAuthoritativeFAMutation` -> `applyWorldMutation` | `evaluateMutationTruth`; mutation validator + league invariants + post-state cap validator | `computeStoreOfferSheetResult` dedup updates in place; later stages compute pure deltas | `persistWorldMutation` Firestore batch | `persisted world truth` | Deterministic `dedupKey` prevents duplicate stored sheets across retries; batch writes team snapshots, player overrides, event, and world metadata in one commit. | No partial world write if batch commit fails. This is one of the stronger parts of the flow. The remaining trust gaps come from incomplete compute outputs, not from the batch envelope itself. | `No issue confirmed` | `src/features/architect/utils/mutationPipeline.ts:1673-2014,4134-4351,4386-4488`; `src/features/architect/GMDashboard/hooks/useArchitectActions.ts:1109-1178`; `src/features/architect/utils/capLegality/postStateCapValidator.ts:227-477`; `src/features/architect/utils/persistenceContracts/contracts.ts:62-114,314-394` |

## Sign-and-Trade flow map

| Stage | Entry file/function | Validation file/function | Compute file/function | Persistence file/function | SSOT status | Resulting state change | Failure-path safety | Stage assessment | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1. Surface availability across entry points | `FreeAgentPool` modal wiring; `GMDashboard` modal wiring; `TradeEditor` SAT modal wiring | Dashboard/free-agent modal surfaces now gate SAT on authoritative preflight availability; Trade Machine SAT modal remains preview-only | None | None | `shared authoritative preflight` for dashboard/free-agent modal surfaces; `local-preview` for Trade Machine SAT modal surface | FreeAgentPool still world-gates SAT. `GMDashboard` now passes SAT commit/preflight callbacks only in world mode, so base-mode dashboard modal paths leave SAT disabled with an explicit reason instead of offering an action the authoritative layer cannot commit. Trade Machine SAT modal remains intentionally preview-only until apply. | No persistence risk at this stage. Base-mode SAT availability drift is no longer confirmed on the dashboard/free-agent modal path. | `No issue confirmed` | `src/features/architect/freeAgency/FreeAgentPool/FreeAgentPool.tsx`; `src/features/architect/GMDashboard/sections/FreeAgencySection.tsx`; `src/shared/components/EditContractModal.tsx`; `src/features/architect/GMDashboard/GMDashboard.tsx`; `src/features/architect/GMDashboard/hooks/useArchitectActions.ts`; `src/features/architect/tradeMachine/TradeEditor.tsx` |
| 2. Payload formation + destination requirement | Direct path: `EditContractModal.handleConfirm` -> `useArchitectActions.handleSignAndTrade`; Trade Machine preview path: `TradeEditor.handleTradeMachineSignAndTrade`; Trade apply path: `useArchitectActions.applyTradeToCapSheet` | Direct path checks destination/non-self/contract shape in `handleSignAndTrade`; Trade Machine preview validates SAT contract via `validateSignAndTradeContractPayload`; trade apply revalidates outgoing SAT assets before `executeTrade` | Direct path uses `ensureContractStructure`; Trade Machine preview uses `setPlayerTrade`; trade apply normalizes SAT payload for pipeline | Direct direct-SAT persistence happens later in `applyWorldMutation`; Trade Machine preview persists only when `executeTrade` is dispatched later | `authoritative mutation` for direct SAT request assembly; `local-preview` for Trade Machine modal assembly | Canonical destination team is required, self-destination is blocked, SAT contract payload is normalized/validated before entering the mutation pipeline. | Safe from partial persistence because these branches are still request-building/preflight stages. I did not confirm a payload-formation bug. | `No issue confirmed` | `src/shared/components/EditContractModal.tsx:723-733,1161-1177`; `src/features/architect/GMDashboard/hooks/useArchitectActions.ts:1935-2053,1428-1554`; `src/features/architect/tradeMachine/TradeEditor.tsx:396-470`; `src/features/architect/utils/tradeMachine/signAndTrade/signAndTradeEligibility.ts:627-730` |
| 3. UI validation preflight | `EditContractModal` -> `useArchitectActions.getSignAndTradePreflight` -> `preflightSignAndTradeMutation`; `useCapValidation` interprets the result synchronously | Shared authoritative SAT preflight; `useCapValidation` only interprets the returned UI-safe status/reasons/warnings | `computeSignAndTradeResult` -> `buildPostTradeTeamsSnapshot` -> `validatePostTradeSnapshotForContext` | None | `shared authoritative preflight` | UI shows loading, blocked, incomplete, and legal SAT state from the same receiving-team post-trade legality model used by the mutation path. | No persistence risk. E6 closes the prior rule drift: the modal no longer guesses from source/current-team apron state, invalidates stale requests immediately, fails closed on incomplete preflight, and only enables confirm when authoritative preflight returns `legal`. Apply-time validation remains the final authority. | `No issue confirmed` | `src/shared/components/EditContractModal.tsx`; `src/features/architect/GMDashboard/hooks/useArchitectActions.ts`; `src/features/architect/hooks/useCapValidation.ts`; `src/features/architect/utils/mutationPipeline.ts`; `src/features/architect/utils/tradeContext/tradeContext.ts`; `src/features/architect/utils/tradeMachine/rules/validateSignAndTrade.ts` |
| 4. Authoritative signing step | `useArchitectActions.handleSignAndTrade` -> `applyWorldMutation('signAndTrade')` | `computeSignAndTradeResult` runs `validateSigning` before continuing | `computeSignAndTradeResult` -> `computeSigningResult` | Not yet persisted at this sub-stage | `authoritative mutation` | Signs the player to the source team in compute-only state, normalizes contract, removes matching source cap hold, and prepares signed player object for trade. | Safe: this step is pure compute inside the mutation pipeline. If signing validation fails, the trade step never runs and nothing is written. | `No issue confirmed` | `src/features/architect/GMDashboard/hooks/useArchitectActions.ts:2026-2041`; `src/features/architect/utils/mutationPipeline.ts:4977-5035,3098-3237,4051-4088` |
| 5. Post-sign trade snapshot + authoritative SAT validation context | `computeSignAndTradeResult` | `validatePostTradeSnapshotForContext` -> `validateTrade` -> `validateSignAndTrade` | `buildPostTradeTeamsSnapshot` | Not yet persisted at this sub-stage | `authoritative mutation` | Builds post-sign/post-trade team snapshots, validates destination routing, Jan. 15 restriction, taxpayer MLE receiver block, contract payload, player eligibility, and receiving-team first-apron hard cap; marks receiving team hard-cap fields in the snapshot. | Safe: still pre-persistence. If trade validation fails, the combined SAT mutation returns failure and nothing commits. I did not confirm a failure-path partial-write bug here. | `No issue confirmed` | `src/features/architect/utils/mutationPipeline.ts:5037-5119`; `src/features/architect/utils/tradeContext/tradeContext.ts:141-213,297-330,392-410,507-528,580-692`; `src/features/architect/utils/tradeMachine/rules/validateSignAndTrade.ts:164-443`; `src/features/architect/utils/tradeMachine/engine/tradeValidator.ts:1121-1170` |
| 6. Persisted SAT result | Direct SAT: `applyWorldMutation('signAndTrade')`; Trade Machine commit: `useArchitectActions.applyTradeToCapSheet` -> `executeTrade` | `validateMutation('signAndTrade')` trusts prevalidated signing + trade context; post-state validator still runs afterward | Direct SAT: `computeSignAndTradeResult` -> `computeTradeResult`; Trade Machine commit: `computeWorldMutation('executeTrade')` -> `computeTradeResult` | `persistWorldMutation` | `persisted world truth` | Team snapshots correctly show source/destination roster movement and receiving-team hard-cap markers after success, and the moved player now persists as a canonical destination-side override while the superseded source-team override path is deleted in the same batch. | Batch commit remains atomic, and the new trade player persistence manifest fails closed if source/destination cannot be resolved conclusively, if a moved player is missing from the final destination snapshot, or if duplicate move candidates resolve to conflicting destinations. The persisted override payload is sourced from the final destination team snapshot, so downstream authoritative `getPlayer` reads stay aligned with the saved team truth. | `No issue confirmed` | `src/features/architect/utils/tradeContext/tradeContext.ts:58-95,114-320`; `src/features/architect/utils/mutationPipeline.ts:2297-2511,2739-3230,4449-4534,5292-5423`; `src/features/architect/utils/teamLoader.ts:251-300,318-352` |
| 7. Failure/rollback envelope | `useArchitectActions.runAuthoritativeFAMutation`; `applyWorldMutation` | `evaluateMutationTruth`; mutation validator + league invariants + post-state cap validator | Pure compute occurs before persistence | `persistWorldMutation` Firestore batch | `persisted world truth` only after commit | No local GM Dashboard team sync happens until authoritative success, and batch commit gates all canonical writes. | Safe from partial write on failure. This remains a strong part of the SAT flow: unresolved routing, failed validation, or persistence failure does not leave behind partial team/player truth. | `No issue confirmed` | `src/features/architect/GMDashboard/hooks/useArchitectActions.ts:936-1189`; `src/features/architect/utils/mutationPipeline.ts:1673-2014,4134-4351`; `src/features/architect/utils/capLegality/postStateCapValidator.ts:227-477` |

## Findings table

## Store Offer Sheet Read Path (E5)

Current read path before E5:

- `handleStoreOfferSheet` sent `teamCode` as the offering team plus `playerId`.
- `loadStateForMutation('storeOfferSheet')` loaded `getPlayer(worldId, offeringTeamCode, playerId)`.
- The loader then inferred `homeTeamCode` from that offering-team-path player object and only afterward loaded the home team.
- Validation and mirrored offer-sheet creation therefore depended on player truth that could be stale, mismatched, or sourced from the wrong team path.

Corrected read path after E5:

- `loadStateForMutation('storeOfferSheet')` now resolves world lineage first and scans only explicit world team snapshots in that lineage.
- Ownership precedence is strict: unique `roster` membership wins, otherwise unique `players[]` membership wins, and any disagreement or ambiguity fails closed.
- The offering team is treated only as the actor. It is never used as the player source-of-truth path.
- After ownership resolves, canonical player truth is built only from the resolved home-team snapshot player plus the first home-team override found in lineage. No base fallback is used to break ambiguity or manufacture player truth.

## Findings table

Trade/sign-and-trade persisted-player-truth is no longer an active finding after E3, the offer-sheet store loader is no longer an active finding after E5, and the SAT modal/preflight drift is no longer an active finding after E6. The table below lists the remaining confirmed issues and drift risks.

| Severity | Finding label | Area | Exact file(s) | Exact function(s) | What is wrong / risky | Why it matters | Layer | Recommended fix direction |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| High | Confirmed risk | Offer Sheet UI validation drift | `src/shared/components/EditContractModal.tsx`; `src/features/architect/hooks/useCapValidation.ts`; `src/features/architect/utils/capLegalityValidation.ts` | `contractDataForValidation`; `useCapValidation`; `validateSigning` | The modal computes legality from `contractDataForValidation`, but that object does not include `rfaOfferSheet`, `rfaOfferSheetOnly`, `rfaOfferSheetStatus`, or any home-team resolution context. The authoritative layer requires those flags/statuses for RFA offer-sheet logic. | The modal can present generic signing legality that does not match the actual authoritative offer-sheet store/finalize rules. | Cross-layer | Pass canonical offer-sheet flags/status into modal validation, or replace modal-side duplication with a shared authoritative preflight helper. |
| Medium | Confirmed risk | Offer Sheet timing enforcement | `src/features/architect/utils/capLegalityValidation.ts`; `src/features/architect/utils/mutationPipeline.ts` | `validateOfferSheetResolution`; `validateMutation` | The 48-hour match window is warning-only. A late match still returns `valid: true` and can persist. | If the product expects the 48-hour window to be a real rule, the current authoritative layer does not enforce it. | Authoritative validation | Promote expired-match-window handling from warning to blocking violation if the business rule is mandatory. |
| Medium | Potential drift risk | Offer Sheet validator messaging | `src/features/architect/utils/capLegalityValidation.ts` | `validateSigning` | The validator still emits `rfa_offer_sheet_stub_active` with the message that the full match/decline workflow is not yet implemented, even though store/match/decline/finalize code paths do exist. | The message is misleading and undermines trust in the current flow. It also makes it harder to tell whether a remaining gap is a real runtime limitation or stale text. | Validation / UX messaging | Replace the stub warning with precise messaging about the real remaining gaps, or remove it if it no longer reflects live behavior. |

## UI Validation vs Authoritative Validation Drift

Is drift currently present?

- Yes.

Where it is confirmed:

- Sign-and-trade modal drift is no longer confirmed after E6. `EditContractModal` now requests a shared authoritative SAT preflight, that preflight uses receiving-team post-trade context, stale responses are discarded, and apply-time validation remains final authority.
- Offer-sheet modal preflight is still drift-prone because the UI never validates the canonical offer-sheet flags/status that the authoritative `validateSigning` logic depends on.
- No-world SAT surface drift is no longer confirmed on the dashboard/free-agent modal path because the modal now treats missing authoritative SAT preflight/commit capability as disabled, explicit, and non-confirmable.

Where drift is only potential:

- `handleSaveContract` is currently fallback-only in the reviewed dashboard path, but if another surface uses it as the primary save handler, it would create committed-looking local truth without authoritative persistence.
- Offer-sheet finalization is no longer an active canonical-shape drift risk after E4, and authoritative offer-sheet storage is no longer a canonical source-truth drift risk after E5. The remaining offer-sheet drift is UI-side preflight that still does not use the authoritative home-team resolution context.

What should be SSOT going forward:

- The mutation pipeline should remain SSOT: `validateSigning`, `preflightSignAndTradeMutation`, `validateOfferSheetResolution`, `buildPostTradeTeamsSnapshot`, `validatePostTradeSnapshotForContext`, `validateSignAndTrade`, and `validatePostStateCapLegality`.
- UI surfaces should not independently re-implement SAT apron logic or offer-sheet resolution logic. They should either call a shared preflight helper built from the authoritative stack or render authoritative preflight results directly.

## Preview/local state truth model

- `handleSaveContract` is explicitly local-only preview code. The comment says it “updates local state only,” and the implementation directly mutates `teamCapSheet` plus `capHolds` without calling `runAuthoritativeFAMutation`, `persistMutation`, or `applyWorldMutation`. Evidence: `src/features/architect/GMDashboard/hooks/useArchitectActions.ts:2655-2735`.
- In the current reviewed GMDashboard path, `handleSaveContract` is not the primary commit path for sign/resign. `EditContractModal` prefers `onSignFreeAgent` / `onResign` before `onSaveContract`, and `GMDashboard` passes both authoritative handlers and the local fallback. Evidence: `src/shared/components/EditContractModal.tsx:1141-1159`; `src/features/architect/GMDashboard/GMDashboard.tsx:188-196,540-556`.
- Trade Machine SAT modal behavior is intentionally preview/local until `applyTradeToCapSheet` dispatches `executeTrade`. `TradeEditor.handleTradeMachineSignAndTrade` only updates local trade state with `setPlayerTrade`; the world write happens later via `useArchitectActions.applyTradeToCapSheet`. Evidence: `src/features/architect/tradeMachine/TradeEditor.tsx:396-470,765-779`; `src/features/architect/GMDashboard/hooks/useArchitectActions.ts:1428-1554`.
- For the current primary free-agency contract path, I did not confirm a “saved-looking local-only commit” bug. Offer-sheet finalization is no longer a primary trust problem after E4, authoritative offer-sheet storage is no longer a primary trust problem after E5, and SAT modal/preflight drift is no longer a primary trust problem after E6. The remaining serious issues are offer-sheet modal preflight drift and warning-only timing enforcement.
- UX messaging is sufficient for the Trade Machine preview branch because that surface is inherently an editor/preview tool. It would not be sufficient if `handleSaveContract` were ever reused as a primary save path, because that handler returns success after local mutation with no built-in preview-only messaging.

## Trust blockers

- Trade/sign-and-trade persisted-player-truth is no longer an active blocker after E3. Remaining blockers are below.
- Offer-sheet modal preflight still does not use authoritative home-team resolution context or authoritative store-only flags/status.
- Offer-sheet resolution still allows late matches because the 48-hour window is warning-only.

## Recommended next ticket list (ordered)

1. Pass canonical offer-sheet flags/status plus authoritative home-team context into modal validation or expose a shared authoritative offer-sheet preflight helper.
2. Make the 48-hour match window a blocking authoritative rule if that is the intended product behavior.
3. Remove or rewrite `rfa_offer_sheet_stub_active` so validator messaging matches live code.

## Validation commands run / intentionally skipped

Commands run:

- `npm run typecheck` — passed.
- `npm run test:node -- --reporter=dot src/tests/architect/useCapValidation.behavior.test.ts src/tests/architect/signAndTrade.test.js` — passed. This covered the new synchronous SAT preflight interpretation path and the authoritative receiver-context / drift-closure SAT proofs.
- `npm run test:ui -- --reporter=dot src/tests/architect/editContractModal.signAndTradePreflight.behavior.test.tsx src/tests/architect/sharedContractPocket.e111.behavior.test.tsx tests/architect/EditContractModal.rules.test.jsx` — passed. This covered SAT loading, invalidation, stale-response handling, and modal disable/warning behavior.
- `npm run test:node -- --reporter=dot src/tests/architect/mutationPipeline.boundary.e107.test.ts src/tests/architect/mutationPipeline.compatibility.guardrail.test.ts src/tests/architect/editContractModal_closure.gate.test.ts src/tests/architect/freeAgency_closure.gate.test.ts src/tests/architect/tradeApply_baseState_authoritativeGate.guardrail.test.ts src/tests/architect/offerSheets_closure.gate.test.ts` — passed. These were updated guardrail checks impacted by the new SAT preflight export surface and the current helper names.
- `npm run test:node -- --reporter=dot` — failed with standing issues outside the E6 SAT alignment change set:
- `tests/contractParser.test.js` — suite failed because `@/shared/utils/contracts/contractParser.js` could not be resolved.
- `src/tests/security/firestoreRules.integration.test.ts` — suite failed because `FIRESTORE_EMULATOR_HOST` was not set under `test:node`.
- `src/tests/security/firestoreRules.integration.test.ts` — teardown also failed because `testEnv` was never initialized after the missing emulator-host precondition.
- `tests/validators/roster.test.js` — 2 failed tests because the suite still expects `warningsOnly: null` while runtime returns `warningsOnly: false`.
- `src/tests/architect/architectTsTopologyCleanup.behavior.test.ts` — 1 timed-out test.
- `src/tests/architect/phase65_forbid_direct_tradeExceptions_reads_guardrail.test.js` — 1 failed source-scan guardrail for direct `.tradeExceptions` read in `src/features/architect/utils/capLegalityValidation.ts`.
- `src/tests/architect/phase83_live_pipeline_mutations_and_season_advance_emulator_e2e.test.js` — 1 failed string-scan gate expecting `advanceSeasonInWorld(worldId` in `src/features/architect/utils/seasonManager.ts`.
- `npm run build` — passed with existing Vite warnings about `fs` browser externalization, mixed static/dynamic imports, stale `caniuse-lite` data, and large chunk sizes.

Intentionally skipped:

- `npm run test:full` was not run because the prompt did not include `RUN FULL SUITE`.
- `npm run validate:project` was not rerun because this ticket did not add new folders, exports, or structural wiring.
- `npm run lint` and `npm run lint:md` were not run because repo policy does not require them for this mutation-layer ticket.
