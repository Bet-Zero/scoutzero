# Architect Teams Plan

**Status:** Router only.

The historical Architect planning/design bundle was physically moved out of the
active docs surface.

## Use This Instead

- For the current live Architect runtime/reference set, start with
  [docs/architect/README.md](../architect/README.md).
- For the archived planning bundle, use
  [archive/docs/architect-teams-plan/README.md](../../archive/docs/architect-teams-plan/README.md).

## What Moved

The archived bundle now contains:

- the original planning docs (`00` through `07`)
- the historical status snapshot
- the full summaries bundle, including the older execution prompt

That material is preserved for design/history context, but it is not the
current implementation source of truth.
