# Architect CBA Canon v2 — Renewed-R9 Targeted R7 Scenario Repair

## Status and exact boundary

The renewed-R9 targeted R7 correction is complete as a maker checkpoint. The
exact input was clean, synchronized report-only commit
`eb43b3b730d9a7fd1857363070838a49487eadbf`; its direct parent and governed
candidate was `9b263272cad2f7643ea394beaa7bdd96cf1a1b97`. Local, tracking,
and fetched live-remote topic refs matched at input with zero ahead and zero
behind. Local and fetched live-remote `main` remained
`69f8f6b6c6f8ea58f1a24eba7949f8ed09744288`.

The maker changed exactly the Canon, repair plan, and this new receipt. No R9
report or prior maker/checker/R8 receipt was modified. This checkpoint is R7
maker completion only. The immediate R8 reconciliation/checksum and its frozen
validator run remain unstarted; renewed R9 also remains unstarted. The Canon is
not accepted and no Phase 2 or product verdict exists.

The repaired Canon is 2,114,372 bytes with SHA-256
`02cc2363b63d2fcb9d2d3a42171cc02a8ed7500888089b85f371874ae073ef04`.
The R7 scenario/crosswalk boundary is 734,090 bytes with SHA-256
`56058b28df76416b9dac1c1bacbbf80e0b3b5e0d2d0c32e393f400ffb0b596bc`.

## Exact checker population

Mechanical extraction begins at the renewed-R9 report's first
`#### Repaired cases` heading and ends immediately before `### SI-C1`. Unique
named-case extraction from that complete material ledger returns exactly 66
identities across 39 scenario families:

`SC-001(h03); SC-005(g,j,l); SC-006(k,l); SC-007(c,e); SC-008(b);`
`SC-009(i); SC-010(b); SC-015(i); SC-016(a,j); SC-017(d); SC-018(a);`
`SC-019(d,g,i); SC-020(a,d); SC-024(a,b,d); SC-028(a,c); SC-032(a,d);`
`SC-033(c); SC-034(b,e,i); SC-035(c); SC-039(e); SC-041(c,l);`
`SC-042(k); SC-044(e); SC-045(a,b,g,h); SC-046(a,b); SC-047(c);`
`SC-053(j); SC-056(a); SC-058(a,c); SC-063(b); SC-070(a,b,d,h);`
`SC-071(c); SC-073(a); SC-075(c); SC-077(e,i); SC-078(a); SC-080(a);`
`SC-081(a,b); SC-082(a,b,c)`.

The population partitions without gap or overlap as follows:

- 65 cases are defects in global-R7 case construction.
- `CBA2-SC-001(h03)` is the one byte-preserved original-R7 mapping defect.
- `CBA2-SC-015(i)` is one of the 65 and also owns the preserved A10.17
  detail/authority dependency correction.

The ordered full-ID digest, sorted lexically and joined with LF, is
`a48405a6139de50f25ad983ac41ee420493213d059899f20c6f339587148e481`.

## Concrete-vector and independent-oracle method

Each identity was re-derived from the active LEAF text, detail input contract,
evidence proposition, dependency chain, and the hash-matched primary passage.
The final row was judged from its completed inputs, not from the maker's desired
PASS/FAIL prose.

The correction method was:

1. Build one complete positive record and one internally possible adverse
   record that changes only the controlling fact or its mechanically coupled
   state fields.
2. Supply exact amounts, percentages, dates, season state, source record,
   dependency result, and lifecycle event required by the registered owner.
3. Calculate every formula independently, retain exact intermediates, and use
   only the signed rounding instruction. A case with missing authenticated data
   returns `NEEDS-INPUT` or `UNAVAILABLE`, never invented FAIL.
4. Remove duplicated scalar/array fields, stale direct-result snapshots,
   contradictory availability/status fields, and asserted legal conclusions.
5. Register every owner needed to reach the result and reconcile the matching
   LEAF detail backlink.

The downloaded official artifacts matched the current authenticated records:

- signed 2023 CBA: 2,850,534 bytes, SHA-256
  `bf178ca0f2d64f9dfe6fde095d3ae43d576b12e19ce7a679618d632584f7ab32`;
- June 2024 By-Laws: 422,247 bytes, SHA-256
  `be4d2781fe8fddfc5bc9028214298f742789a949dade4ead26368a4336d32ccf`.

The source reread included CBA Exhibit A, Exhibit 4; CBA Articles II §§7, 9,
and 11; VII §§2(d), 2(f), 3(a)–(b), 4(a), 5(a), 7(a), and 8; XI §4(c);
XXIV §2(a); XXIX §6; and By-Laws §§4.04(c) and 6.06(d). No accepted source or
evidence proposition required repair.

## Complete per-identity disposition

| Identity | Renewed-R9 finding closed by final correction |
|---|---|
| `CBA2-SC-001(h03)` | Removed the false Row-H/A05.10 mapping, authority, Exercise, detail backlink, and result clause while preserving the Standard-TPE case and A02.1/A02.2 result content. |
| `CBA2-SC-005(g)` | Added the A04.33 By-Laws reallocation owner and made no-agreement default versus express assignee agreement one complete atomic pair. |
| `CBA2-SC-005(j)` | Added A04.12 and exact original/extended-term dates so original-term calculation and extended-term inapplicability resolve separately. |
| `CBA2-SC-005(l)` | Synchronized the A04.1 result with the 15.0001% adverse form and retained the exact $3,000,020 result. |
| `CBA2-SC-006(k)` | Added signing/trade dates, original/extended years, replacement Exhibit 4 term, Base Compensation, and protected percentages. |
| `CBA2-SC-006(l)` | Replaced legal-conclusion prose with exact Extension, Exhibit 4, trade-date, term, and Base Compensation facts. |
| `CBA2-SC-007(c)` | Corrected 15.00005% of $20 million to $3,000,010 and the exact $10 ceiling excess. |
| `CBA2-SC-007(e)` | Made the adverse A04.1 snapshot FAIL with the prohibited $0.01 increase. |
| `CBA2-SC-008(b)` | Made the adverse A04.1 snapshot FAIL when the dollar form exceeds its cap by $0.01. |
| `CBA2-SC-009(i)` | Made A02.6 PASS only in 2023-24 and FAIL in the 2024-25 Transition-TPE record. |
| `CBA2-SC-010(b)` | Removed the stale Row-K/TMLE-use PASS when the adverse use history is empty. |
| `CBA2-SC-015(i)` | Replaced the irrelevant A10.4 chain with A10.17 in the body, authority, Exercises, dependency detail, and backlink. |
| `CBA2-SC-016(a)` | Changed only Option exercise state and stopped A10.14 from deciding the separate non-exercised A10.29 branch. |
| `CBA2-SC-016(j)` | Used a first extended Season whose proposed dates are truly more than one year early and exact offseason/Regular-Season boundary dates. |
| `CBA2-SC-017(d)` | Replaced the stale exercise-only A10.14 PASS with the truthful not-exercised result while A10.29 tests two versus one non-Option Seasons. |
| `CBA2-SC-018(a)` | Changed the underlying incoming Salary by $0.01 so no legal disjoint decomposition remains, instead of merely duplicating a proposed assignment. |
| `CBA2-SC-019(d)` | Moved the last Regular Season game into the stated 2026-27 Salary Cap Year and retained the exact 2034 selection result. |
| `CBA2-SC-019(g)` | Synchronized A12.5 and the unfreeze event across the one-above versus two-above histories. |
| `CBA2-SC-019(i)` | Made A12.6 FAIL for the omitted challenged term and PASS when that same term is disclosed. |
| `CBA2-SC-020(a)` | Made the C01.8 removal owner false before signing and true after the exact signing event. |
| `CBA2-SC-020(d)` | Removed the invented three-YOS gate and directly tested the `min(applicable minimum, two-YOS minimum)` amount. |
| `CBA2-SC-024(a)` | Replaced the placeholder with an exact signing date, authenticated 174-day season, covered-day count, YOS, term, and minimum amount. |
| `CBA2-SC-024(b)` | Removed conflicting current-salary fields and kept one internally consistent YOS/minimum record per side. |
| `CBA2-SC-024(d)` | Kept the two-Season facts fixed and tested separate versus improperly merged exception/reimbursement outputs. |
| `CBA2-SC-028(a)` | Added Tax Team Salary, Tax Level, exact $24,256,000 excess, four operands, C09.7 Exercise/authority, and exact progressive sum. |
| `CBA2-SC-028(c)` | Added the same complete repeater-tax inputs, C09.7 chain, and exact progressive sum. |
| `CBA2-SC-032(a)` | Rejects only the submitted combined allocation and explicitly preserves the valid single-TMLE alternative. |
| `CBA2-SC-032(d)` | Replaced the impossible $10 million BAE with the authenticated $5.477 million BAE while preserving DPE non-proration as the direct classification. |
| `CBA2-SC-033(c)` | Replaced `minimum` placeholders with exact guaranteed and Option salaries and removed the Option salary when the Option is absent. |
| `CBA2-SC-034(b)` | Split Salary excluding incentives, Regular Salary, and the individual Unlikely Bonus; the adverse changes only that bonus above its 5% limit. |
| `CBA2-SC-034(e)` | Supplied the same complete component vector for the Room MLE annual-change test. |
| `CBA2-SC-034(i)` | Supplied the same complete component vector for the BAE annual-change test. |
| `CBA2-SC-035(c)` | Supplied the same complete component vector for the NTMLE annual-change test. |
| `CBA2-SC-039(e)` | Removed the conflicting scalar year-two field and changed the value inside the single retained schedule. |
| `CBA2-SC-041(c)` | Kept a 65-game season on both sides and changed qualifying games from 65 to 64. |
| `CBA2-SC-041(l)` | Kept one exact authenticated Regular-Season date and changed only remaining Seasons from one to two. |
| `CBA2-SC-042(k)` | Added the proposed amount to P+, added it to the detail input contract, and marked only the applicable YOS and amount owners as selected. |
| `CBA2-SC-044(e)` | Replaced protected-Salary weights with the signed prior-year Salary weights and showed the exact 1:2 allocation. |
| `CBA2-SC-045(a)` | Added annual Base Compensation, protected dollars, and protected percentages; allocation uses the signed percentages. |
| `CBA2-SC-045(b)` | Added the same complete protection basis and a complete allocation array with the ETO Season excluded. |
| `CBA2-SC-045(g)` | Added cap state, payment boundary, Base Compensation, protected dollars, and protected percentages for the extended-only branch. |
| `CBA2-SC-045(h)` | Added the full original-plus-extended Base Compensation and protected-percentage schedule for the early below-cap branch. |
| `CBA2-SC-046(a)` | Made one possible ordinary-signing vector: C18.1/C18.2 apply, while zero-protection and trade-earned branches are explicitly not applicable. |
| `CBA2-SC-046(b)` | Added extended Base Compensation and protected percentages and derived the 1:2 allocation from 50% versus 100%. |
| `CBA2-SC-047(c)` | Used an authenticated comparison minimum whose ten-day proration is exactly $120,000, eliminating a falsely exact truncated rational. |
| `CBA2-SC-053(j)` | Supplied zero Room in P+ and changed only the available Minimum Exception by $0.01 in P−. |
| `CBA2-SC-056(a)` | Removed duplicated percentage scalars and derives both percentages from the single Base/protected arrays. |
| `CBA2-SC-058(a)` | Removed the stale total field and derives $4,000,000.00 versus $4,000,000.01 from the components. |
| `CBA2-SC-058(c)` | Removed stale percentage fields and derives 20.0000000% versus 20.00000004% from exact amounts. |
| `CBA2-SC-063(b)` | Replaced the aggregate-only input with per-year remaining unearned protected Compensation and the exact 6:4:2 allocation. |
| `CBA2-SC-070(a)` | Derives actual combined count and bounds from Standard and Two-Way counts with no contradictory scalar. |
| `CBA2-SC-070(b)` | Uses the signed 14-consecutive/28-total limits and changes the Season total from 28 to 29. |
| `CBA2-SC-070(d)` | Replaced overlapping available/unavailable lists with one player-by-player map and derives eight versus seven. |
| `CBA2-SC-070(h)` | Uses the same single availability map for the direct eight-player rule. |
| `CBA2-SC-071(c)` | Replaced game-count proration with exact 174 total and 88 remaining Regular-Season days; 25.287… rounds to 25. |
| `CBA2-SC-073(a)` | Supplies an authenticated controlling G League rule in P+ and returns `NEEDS-INPUT` when that record is absent. |
| `CBA2-SC-075(c)` | Makes Room exactly $10 million and changes only the proposed increase to $10,000,000.01 in P−. |
| `CBA2-SC-077(e)` | Removed the stale starter boolean and derives the branch from 41/2,000 versus 40/1,999. |
| `CBA2-SC-077(i)` | Removed asserted consequences from the inputs and derives effective July 13 withdrawal versus ineffective July 14 withdrawal without consent. |
| `CBA2-SC-078(a)` | Returns PASS for TEAM-B when the official record selects TEAM-B and separately states that TEAM-A has no right. |
| `CBA2-SC-080(a)` | Corrected inverse winning-percentage order to TEAM-C, TEAM-B, TEAM-A from earlier to final selection. |
| `CBA2-SC-081(a)` | Returns `UNAVAILABLE/NEEDS-INPUT` for unresolved protection with no availability verdict. |
| `CBA2-SC-081(b)` | Removed the stale adjacent-pair boolean and derives each pair from one retained-rights schedule. |
| `CBA2-SC-082(a)` | Returns `NEEDS-INPUT` and no Tax Bracket amount for a mismatched source year. |
| `CBA2-SC-082(b)` | Returns `NEEDS-INPUT` and no cash-limit amount for a mismatched source year. |
| `CBA2-SC-082(c)` | Overrides the original `unrounded` field itself, so P− no longer declares a second missing field while retaining the value. |

## Exercise, backlink, and coverage reconciliation

The baseline contained 955 unique case-to-LEAF Exercise edges and 955 exact
detail backlinks. The targeted changes are exhaustive:

- remove `CBA2-SC-001(h03)` → `CBA2-A05.10`;
- add `CBA2-SC-005(g)` → `CBA2-A04.33`;
- add `CBA2-SC-005(j)` → `CBA2-A04.12`;
- add `CBA2-SC-015(i)` → `CBA2-A10.17`;
- add `CBA2-SC-028(a)` → `CBA2-C09.7`; and
- add `CBA2-SC-028(c)` → `CBA2-C09.7`.

The final population is therefore 959 unique Exercise edges and 959 exact
matching backlinks. Every pair resolves bidirectionally. The additions and
removal involve already-covered SCEN owners, so coverage remains exactly 581
primary plus 80 secondary-only SCEN LEAFs, 661 total. Scenario and case
identities remain 82 / 728. The historical scenario crosswalk remains 21,909
bytes with SHA-256
`08c5ed69be1075ad7a9909337b09be30b7d43dcf4114188bee142d64c468d787`.

## Preservation proof

An exact row comparison against `9b263272` proves:

- exactly the ledger's 66 named case rows changed;
- the other 662 named case rows are byte-identical;
- the 662-row ordered-byte digest is
  `2fb552235818a90d0e0ea51a26ed99239fefd571d6bbd612fdcf7ba1d7d82fe0`;
- exactly seven detail rows changed: `A04.12`, `A04.33`, `A05.10`, `A10.9`,
  `A10.17`, `C09.7`, and `C16.35`; and
- every other governed table row is byte-identical.

The 3,764 protected governed table rows outside those 73 authorized rows are
byte-identical, with ordered digest
`d28d08b0156bbd2c74dda43467e1c82767dace884c6a888c1b6b697d0cbf87b6`.
That proves no accepted main rule, source, evidence proposition, locator, XW2
or SXW2 edge, lineage record, historical fragment, terminal disposition,
validator field, or frozen route field changed. The unchanged validator remains
499,315 bytes with SHA-256
`e65103466154162f2f86b0526ce67e4be9fa394b54bc2fd71a7c1cd01d46c461`.
The frozen route checksum remains
`b0c97d74d1426a323101155d61ebb86d2c42d6f66023ba383173e797db0a8cc1`.

## Validation attempts and results

- Complete 66-case semantic re-derivation and contradiction/stale-field scan:
  PASS. The mechanical audit reports the exact 66 / 39 ledger, 662 preserved
  rows, 959 / 959 Exercise/backlink reconciliation, and every protected-row
  assertion above.
- Authenticated primary-source hash and targeted passage review: PASS. Both
  official artifacts matched their registered SHA-256 values.
- Targeted Markdown lint across the exact three authorized files, with inherited
  `MD029` treatment: PASS; the exact final candidate was repeated after this
  evidence was recorded.
- Targeted `npm run test:diff -- --files <the three authorized paths>
  --reporter=dot`: PASS; the approved runner selected `FAST`, and all 12 test
  files / 57 tests passed. The exact final candidate was repeated after this
  evidence was recorded.
- `npm run docs:guardrails`: PASS; repeated on the exact final candidate.
- `npm run validate:project`: PASS; repeated on the exact final candidate.
- `git diff --check`: PASS; repeated on the exact final candidate.
- Frozen validator: intentionally not run for Stage 1; the immediately
  following R8 checkpoint owns the final integrated certification.
- Full suite, build, typecheck, application tests, E2E, Graphify regeneration,
  and unrelated checks: intentionally not run because this is the bounded
  documentation/Canon correction and those commands are explicitly excluded.

No R8 completion, renewed R9, owner acceptance, Phase 2, application change,
Linear action, Graphify regeneration, merge, or `main` change is claimed.
