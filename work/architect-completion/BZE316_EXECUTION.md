# BZE-316 supplied penalized-first relative order

## Scope and authority

The sustained September 12 owner authorization permits this next single rule
component after the accepted saved-world synthetic path. Base main is
`6437018037a1314a7aa54684b7a8817144c81df1`; it exactly contains the accepted
positive-path candidate. Required post-merge CI34736947077 passed. Main and
origin were clean and synchronized before branch creation.

Fresh accepted lookup: `npm run architect:canon:lookup -- CBA2-L08.8`.
Pin `6cf8aaf358c158a88e630e8a7336f7e9c3febc17`, artifact SHA256
`23fe883f6f1aec7799fc3396bef404c250fd26beefa705582a5307766ad7ff76`.
SC080(a)'s expectation, recorded before tests: .400 precedes .500 precedes .600;
the reversed order fails. Membership and percentages are supplied qualified facts,
not inferred penalty eligibility or real NBA observations.

Concrete failures prevented: reversed signed order; omitted penalized picks;
wrong original identity/year; stale or unqualified facts; floating-point ties
or invented tie breaks; relative order promoted to final placement or permission.
The smallest meaningful proof is the Canon pair plus discriminating membership,
context, qualification, exact-decimal and tie cases. A schema-generator pass alone
cannot establish any of these semantics.

## Existing mechanisms inspected

Graphify was queried and its JSON parsed before editing. Its pre-edit build at
accepted candidate `2e726355` has an identical tree to base `64370180`; the
merge introduces no missing source topology. Current source confirmed the paths.

- Existing Apron code computes trigger/freeze/unfreeze/penalty/no-penalty;
  placement is explicitly blocked. No L08.8 relative-order implementation existed.
- Rollover/conveyance, election/replacement term schemas and swap adapters already
  exist. Fresh CBA2-L09.2 supplies no operational unknown-program algorithm.
  Legacy description parsing, assumed ties and default roll/cancel behavior are
  not accepted W10 authority. Complete qualified controlling terms and identities
  remain required; no generic unknown-clause interpreter is authorized.
- Existing entitlement claims and full-league uniqueness gates are reused by the
  accepted trade path. No duplicate claim engine is introduced here.
- Season Advance already uses an atomic all-team transition and preserves draft
  entitlements without a verdict, or blocks a required unavailable transition.
  Its missing governed draft-history inputs remain explicit. This component does
  not replace that boundary or automatically replay any source observation.
- Fresh L08.3 establishes persistence of an accepted freeze event only. It does
  not authorize recomputing a verdict or missing lifecycle inputs. No freeze-event
  persistence extension is included in this relative-order change.

The twelve unresolved pool identities, missing cutoff/drawing procedures and
future post-2029 method remain separate. No new source route appeared or was used.
Future outcomes are not research targets. W10 still requires all 278 assets.

## Implementation boundary

New schema, pure evaluator and focused tests; foundation documentation updated.
Reuse existing original-pick identity, operation context, scoped fact selection
and accepted-source/effective-date checks without changing predecessor behavior.
Decimal text compares exactly; ties always need input. Outputs are immutable,
JSON-safe, component-only, with Apply blocked and final placement not evaluated.
No mutation, UI, source collection, baseline or production activation change.

The accepted synthetic path's independent review observations remain in its
landing receipt. In particular, complete collection membership must be fenced
before non-synthetic/live expansion; this pure component neither fixes nor waives
that prerequisite. The accepted browser proof is reused only for its original
finite synthetic scope.

## Validation and landing

Initial scoped command:
`npm run test:node -- tests/architect/draftPenaltyOrder.test.ts --reporter=dot`
passed all 46 cases in 4.94 seconds. Further required checks and exact candidate
receipts are recorded on the issue and PR before independent review.

`npm run typecheck`, `npm run validate:project`, `npm run docs:guardrails`,
scoped Markdown lint of both changed documents and `git diff --check` passed.
The first project invocation failed only because the sandbox denied TSX's local
IPC socket; the permitted retry passed. `npm run schema:check` passed its legacy
generator scope only. Its three untracked boilerplate outputs were retained
privately and removed; they do not describe or certify the new Zod contract.

Skipped: new browser/UI proof and local build because no rendered or persistence
behavior changes; the hosted build remains a required gate. No broad local suite,
lint cleanup, source research, production pipeline or source writes. The one
manual full-suite allowance remains reserved for a genuinely final integrated
draft-pick candidate. This component alone is not that candidate.

Independent focused Claude ACCEPT, exact-head green CI, settled threads, normal
merge and post-merge main verification remain mandatory. Author tests are not
independent acceptance; Stage B, W10 and production activation stay open.
