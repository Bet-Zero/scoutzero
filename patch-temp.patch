--- a/src/utils/architect/tradeMachine/tradeValidator.js
+++ b/src/utils/architect/tradeMachine/tradeValidator.js
@@ -43,12 +43,9 @@ const validateTradeExceptions = (team) => {
-    if (team.postTradeStatus?.isAtOrAboveSecondApron) {
-      if (!isCurrentSeasonTPE(tpe, yearKey)) {
-        addBlock();
-        return;
-      }
-    }
+    if (team.postTradeStatus?.isAtOrAboveSecondApron) {
+      addBlock();
+      return;
+    }
   }
 
   // ...snip...
@@ -67,11 +64,9 @@ const validateTradeExceptions = (team) => {
-  if (
-    team.postTradeStatus?.isAtOrAboveSecondApron &&
-    Array.isArray(team.appliedTPEs)
-  ) {
-    if (team.appliedTPEs.some((tpe) => !isCurrentSeasonTPE(tpe, yearKey))) {
-      addBlock();
-    }
-  }
+  if (team.postTradeStatus?.isAtOrAboveSecondApron && Array.isArray(team.appliedTPEs)) {
+    if (team.appliedTPEs.length) {
+      addBlock();
+    }
+  }
